---
import BaseLayout from '../layouts/BaseLayout.astro';
import FeeTable from '../components/FeeTable.astro';
import { fees } from '../lib/fees';
---

<BaseLayout
  title="取扱分野"
  description="TODO: 現行 /service ページのmeta descriptionを移植してください。"
>
  <h1>取扱分野</h1>

  <p>TODO: 現行 /service ページの本文を移植してください。</p>

  {
    fees.categories.map((category) => (
      <section>
        <h2>{category.name}</h2>
        <p>TODO: この分野の解説文。</p>
        {/* 料金は fees.yaml から描画されるため、ここに金額を書かないこと */}
        <FeeTable category={category} />
      </section>
    ))
  }
</BaseLayout>

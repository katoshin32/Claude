# ダイナミックフロー・ワークスペース

このリポジトリは、Claude Code を「業務の定型作業の自動化」と「ドキュメントの品質担保」に
活用するための設定・フロー集です。CLI からでも Web/モバイル（claude.ai/code）からでも
同じフローが動くよう、設定はすべてプロジェクト配下（`.claude/`）にコミットしています。

## 接続している外部サービス（MCP）

| サービス | 主な用途 |
|----------|----------|
| Notion   | ドキュメント・議事録・ナレッジの保存先 |
| Gmail    | メールの検索・下書き作成・ラベル整理 |
| Google Drive | ファイルの検索・読み込み・作成 |
| Google Calendar | 予定の参照・週次の活動集計 |
| GitHub   | PR/Issue の参照・レビュー・自動対応 |
| Asana    | タスク・プロジェクトの参照と作成 |

> サービス名（MCP サーバ名）は環境によって変わるため、フロー定義では特定のツール名を
> ハードコードせず「Notion に保存」「Gmail を検索」のように意図で記述しています。

## 基本方針

- **出力は必ず人間がレビューできる形で**：メール送信・PR マージ・予定削除など外向き・不可逆な
  操作は、実行前に必ず確認する。下書き（draft）止まりを既定とする。
- **保存先の既定**：構造化ドキュメント → Notion、ファイル成果物 → Google Drive、
  ソースコード・設定 → この Git リポジトリ。
- **言語**：成果物・コミットメッセージは日本語を既定とする。

## ディレクトリ構成

- `.claude/commands/` … スラッシュコマンド（再利用する定型フロー）
- `.claude/agents/`   … サブエージェント（専門特化の処理担当）
- `.claude/settings.json` … 権限・フックの設定
- `docs/`            … 設計ドキュメント

## よく使うフロー

- `/weekly-report` … カレンダー・メール・GitHub・Asana から週次活動をまとめる
- `/meeting-notes` … 生メモを構造化された議事録にして Notion へ
- `/doc-review`    … ドキュメントの品質レビュー
- `/inbox-triage`  … 受信メールの分類とラベル付け（下書きのみ）
